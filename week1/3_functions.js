// Basic named
function foo(x, y) {
    return x + y;
}

// Function expression
const foo = function(x, y) {
    return x + y;
}

// Arrow function
const foo = (x, y) => {
    return x + y;
}

// Arrow function with implicit return
const foo = (x, y) => x + y;